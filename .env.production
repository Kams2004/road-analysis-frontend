# Server-side (Next.js API proxy — runs inside Docker, reaches host services via host IP)
API_URL=http://185.182.184.188:8080
MINIO_URL=http://185.182.184.188:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Browser-side (baked in at build time — must be the public server IP)
NEXT_PUBLIC_API_URL=http://185.182.184.188:8080
