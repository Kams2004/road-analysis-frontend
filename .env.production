# Server-side (Next.js API proxy — runs on the server, reaches API via localhost)
API_URL=http://127.0.0.1:8080
MINIO_URL=http://127.0.0.1:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Browser-side (baked in at build time — must be the public server IP)
NEXT_PUBLIC_API_URL=http://185.182.184.188:8080
