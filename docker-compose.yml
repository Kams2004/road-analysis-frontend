services:
  frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://185.182.184.188:8080
    env_file: .env.production
    network_mode: host
    restart: unless-stopped
